# !/bin/sh
#

for i in $@
do

        FILE=`echo $i | cut -d':' -f1`
        LINE=`echo $i | cut -d':' -f2`

        if [ "${LINE}" == "${FILE}" ]; then LINE=0; fi

        emacsclient --no-wait +$LINE $FILE
done
# > /dev/null &
