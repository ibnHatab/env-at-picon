#name : defmodule
#group : definitions
# --
# File    : ${1:`(file-name-nondirectory (or (buffer-file-name) (buffer-name)))`}

defmodule ${2:`(capitalize (file-name-nondirectory
              (file-name-sans-extension (or (buffer-file-name) (buffer-name)))))`} do
    @moduledoc """
    Provides a $3
  """
    $0

end